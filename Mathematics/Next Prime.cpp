#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

ll nextPrime(ll n)
{
    if (n == 1)
        return 2;
    if (n == 2)
        return 3;
    if (n < 5)
        return 5;
    
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin >> t;
    while (t--)
    {
    }

    return 0;
}